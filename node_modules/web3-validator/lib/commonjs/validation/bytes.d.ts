import { ValidInputTypes } from '../types';
/**
 * checks input if typeof data is valid buffer input
 */
export declare const isBuffer: (data: ValidInputTypes) => boolean;
export declare const isBytes: (value: ValidInputTypes | Uint8Array | number[], options?: {
    abiType: string;
    size?: never;
} | {
    size: number;
    abiType?: never;
}) => boolean;
